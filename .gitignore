# build output is committed (index.html) so GitHub Pages can serve it directly.
# Ignore local scratch, deps and test artifacts:
node_modules/
vendor/
*.png
# …but the PWA icons ARE part of the deployed app — keep them tracked:
!icon-192.png
!icon-512.png
!icon-maskable-512.png
!apple-touch-icon.png
test-local.html
.DS_Store
